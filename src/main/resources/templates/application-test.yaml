Spring:
  name: usuarioTest

  datasource:
    url: jdbc:h2:mem:testesdb
    username: sa
    password: 123456
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true